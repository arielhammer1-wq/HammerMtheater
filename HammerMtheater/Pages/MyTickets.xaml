<Page x:Class="HammerMtheater.Pages.MyTickets"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#0A0A0A">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <Button HorizontalAlignment="Left" 
                    Background="Transparent" 
                    BorderThickness="0" 
                    Cursor="Hand"
                    Click="Back_Click"
                    Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="←" Foreground="#E50914" FontSize="20" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="BACK TO MOVIES" Foreground="#666" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="12"/>
                </StackPanel>
            </Button>

            <TextBlock Text="My Digital Wallet"
                       Foreground="#E50914" 
                       FontSize="12" 
                       FontWeight="Bold" 
                       Margin="0,0,0,5"/>
            <TextBlock Text="Your Tickets"
                       FontSize="36"
                       FontWeight="Black"
                       Foreground="White"/>
        </StackPanel>

        <ListBox x:Name="TicketsList"
                 Grid.Row="1"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.VerticalScrollBarVisibility="Hidden"
                 HorizontalContentAlignment="Stretch">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,20"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#161616" CornerRadius="15" BorderBrush="#222" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" >
                                <TextBlock Text="{Binding Movie.MovieName}"
                                           Foreground="White" FontSize="20" FontWeight="ExtraBold"/>

                                <WrapPanel Margin="0,10,0,0">
                                    <StackPanel Margin="0,0,20,0">
                                        <TextBlock Text="THEATER" Foreground="#444" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Theater.NameOfTheater}" Foreground="#BBB" FontSize="13"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="HALL" Foreground="#444" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Hall.HallName}" Foreground="#BBB" FontSize="13"/>
                                    </StackPanel>
                                </WrapPanel>
                            </StackPanel>

                            <Rectangle Grid.Column="1" Width="1" Fill="#222" Margin="0,15" StrokeDashArray="4 2"/>

                            <StackPanel Grid.Column="2" Background="#1A1A1A" VerticalAlignment="Stretch">
                                <TextBlock Text="SEAT" Foreground="#444" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding SeatNumber}" Foreground="White" FontSize="24" FontWeight="Black" HorizontalAlignment="Center"/>

                                <Border Background="#251010" CornerRadius="8" Margin="0,10,0,0" Padding="5">
                                    <TextBlock Text="{Binding TicketPrice, StringFormat='₪{0}'}" 
                                               Foreground="#E50914" 
                                               FontSize="16" 
                                               FontWeight="Bold" 
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>