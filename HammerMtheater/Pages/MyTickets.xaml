<Page x:Class="HammerMtheater.Pages.MyTickets"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Background="#050505">

    <Grid>
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,30">
                <Button HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Back_Click" Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ArrowLeft" Foreground="#E50914" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="BACK TO HOME" Foreground="#888" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="12" />
                    </StackPanel>
                </Button>

                <TextBlock Text="MY DIGITAL WALLET" Foreground="#E50914" FontSize="12" FontWeight="Black"  Margin="0,0,0,5"/>
                <TextBlock Text="Your Tickets" FontSize="48" FontWeight="Black" Foreground="White"/>
            </StackPanel>

            <ListBox x:Name="TicketsList" Grid.Row="1" Background="Transparent" BorderThickness="0" 
                     ScrollViewer.VerticalScrollBarVisibility="Hidden" HorizontalContentAlignment="Stretch">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#CC161616" CornerRadius="20" BorderBrush="#33FFFFFF" BorderThickness="1" Margin="0,0,0,20">
                            <Grid Height="120">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"  VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Movie.MovieName}" Foreground="White" FontSize="24" FontWeight="Black"/>
                                    <WrapPanel Margin="0,10,0,0">
                                        <StackPanel Margin="0,0,30,0">
                                            <TextBlock Text="THEATER" Foreground="#666" FontSize="10" FontWeight="Black"/>
                                            <TextBlock Text="{Binding Theater.NameOfTheater}" Foreground="#BBB" FontSize="14"/>
                                        </StackPanel>
                                        <StackPanel>
                                            <TextBlock Text="HALL" Foreground="#666" FontSize="10" FontWeight="Black"/>
                                            <TextBlock Text="{Binding Hall.HallName}" Foreground="#BBB" FontSize="14"/>
                                        </StackPanel>
                                    </WrapPanel>
                                </StackPanel>

                                <Rectangle Grid.Column="1" Width="2" Fill="#33FFFFFF" Margin="0,20" StrokeDashArray="2 2"/>

                                <StackPanel Grid.Column="2" Background="#11FFFFFF"  HorizontalAlignment="Stretch" Orientation="Vertical" VerticalAlignment="Center">
                                    <TextBlock Text="SEAT" Foreground="#E50914" FontSize="10" FontWeight="Black" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding SeatNumber}" Foreground="White" FontSize="36" FontWeight="Black" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding TicketPrice, StringFormat='₪{0}'}" Foreground="#888" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Background="#F2050505">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" 
                             Width="60" Height="60" Foreground="#E50914" />
                <TextBlock Text="SCANNING WALLET..." Margin="0,20,0,0" Foreground="White" FontSize="12" FontWeight="Black" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>