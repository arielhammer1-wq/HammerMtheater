<Page x:Class="HammerMtheater.Pages.MoviePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:HammerMtheater.UserControls"
      Background="#050505">

    <Grid>
        <Image Source="C:\Users\User\Downloads\HammerMtheater-master\HammerMtheater-master\HammerMtheater\Assets\background_static.jpg" 
               Stretch="UniformToFill" 
               Opacity="0.8">
            <Image.Effect>
                <BlurEffect Radius="10"/>
            </Image.Effect>
        </Image>

        <Border IsHitTestVisible="False">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF050505" Offset="0"/>
                    <GradientStop Color="#99000000" Offset="0.4"/>
                    <GradientStop Color="#FF050505" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Canvas IsHitTestVisible="False">
            <Ellipse Width="800" Height="800" Fill="#15E50914" Margin="-300,-300,0,0">
                <Ellipse.Effect>
                    <BlurEffect Radius="150"/>
                </Ellipse.Effect>
            </Ellipse>
        </Canvas>

        <Grid Margin="40,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,40">
                <TextBlock Text="BROWSE CATALOG" Foreground="#E50914" FontSize="12" FontWeight="Black" Margin="20,0,0,10"/>
                <Border Background="#22FFFFFF" CornerRadius="25" BorderBrush="#33FFFFFF" BorderThickness="1" 
                        MaxWidth="600" HorizontalAlignment="Left" Margin="10,0">
                    <Grid>
                        <materialDesign:PackIcon Kind="Magnify" Foreground="#888" VerticalAlignment="Center" Margin="20,0,0,0" Width="22" Height="22"/>
                        <TextBox x:Name="SearchBox"
                                 BorderThickness="0" Background="Transparent"
                                 Foreground="White" Padding="55,15,25,15"
                                 FontSize="16"
                                 VerticalContentAlignment="Center"
                                 materialDesign:HintAssist.Hint="Search movies, genres, or directors..."
                                 materialDesign:HintAssist.Foreground="#555"
                                 TextChanged="SearchBox_TextChanged"/>
                    </Grid>
                </Border>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" PanningMode="Both">
                <ItemsControl ItemsSource="{Binding Genres}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,45">
                                <StackPanel Orientation="Horizontal" Margin="20,0,0,20">
                                    <Rectangle Width="4" Height="24" Fill="#E50914" Margin="0,0,12,0"/>
                                    <TextBlock Text="{Binding GenreName}"
                                               FontSize="28" FontWeight="Black"
                                               Foreground="White" VerticalAlignment="Center"/>
                                </StackPanel>

                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding Movies}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <controls:MovieCard/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>